<UserControl x:Class="EasyRouter.Views.Connecting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Path=RouterModel}" HorizontalAlignment="Center" FontSize="24" />
            <Image Source="{Binding Path=RouterImageUri}" />
            <TextBlock Text="Connecting..." HorizontalAlignment="Center" FontSize="24" />
        </StackPanel>
    </Grid>
</UserControl>
