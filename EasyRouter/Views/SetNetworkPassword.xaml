<UserControl x:Class="EasyRouter.Views.SetNetworkPassword"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="314" d:DesignWidth="404">
    <StackPanel>
        <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="20" Text="Choose a private" />

        <TextBlock HorizontalAlignment="Center" Margin="0,0,0,0" FontSize="20" Text="WiFi password:" />
        <TextBox Text="{Binding Path=NetworkPassword, Mode=TwoWay}"  Padding="10" Margin="20" Background="#FFFFB500" FontSize="30" />
        <Button Command="{Binding Path=NextCommand}" HorizontalAlignment="Right" Background="White" Margin="20" BorderBrush="White">
            <Image Source="/Content/Arrow.jpg" Width="81" Height="35" />
        </Button>
    </StackPanel>
</UserControl>
