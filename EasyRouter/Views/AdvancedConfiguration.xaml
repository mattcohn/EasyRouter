<UserControl x:Class="EasyRouter.Views.AdvancedConfiguration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="314" d:DesignWidth="404">
    <StackPanel>
        <Image Source="/Content/EasyRouter8Top.jpg" />
        <TextBlock FontSize="16">WiFi Name:</TextBlock>
        <TextBox Text="{Binding Path=NetworkName, Mode=OneWay}"  Padding="3" Margin="5" Background="Orange" FontSize="20" />
        <TextBlock FontSize="16">WiFi Password:</TextBlock>
        <TextBox Text="{Binding Path=NetworkPassword, Mode=OneWay}"  Padding="3" Margin="5" Background="Orange" FontSize="20" />
        <Image Source="/content/EasyRouter8Bottom.jpg" />
    </StackPanel>
</UserControl>
